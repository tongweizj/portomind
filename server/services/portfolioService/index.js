// const rebalanceSettings = require('./rebalanceSettings');
const positionTracker   = require('./positionTracker');
// 其他子模块导入...

module.exports = {
  // 原有功能
//   rebalanceSettings,
  // 新增持仓分析子模块
  positionTracker
};
